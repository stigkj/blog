<%
output = ''
all_articles.select { |a| a[:alt_url] }.each do |a|
	output += "Redirect 301 #{a[:alt_url]} #{@config[:base_url]}#{a[:source_url]}\n"
	output += "Redirect 301 #{a[:alt_url2]} #{@config[:base_url]}#{a[:source_url]}\n" if a[:alt_url2]
end
%><%= output %>
